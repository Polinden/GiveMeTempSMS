name: Newman Run

on:
  push:
    branches:
    - master

jobs:
  newman:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: matt-ball/newman-action@master
        with:
           apiKey: ${{ PMAK-61c6df6aeb51ae005130e251-95851870cf098947a518f94bc478143864 }}
           collection: ac0ecb85f21d0821c3df
           reporters: cli
      - name: Output summary to console
        run: echo ${{ steps.run-newman.outputs.summary }}
